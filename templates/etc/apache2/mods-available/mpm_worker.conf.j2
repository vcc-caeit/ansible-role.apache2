{{ ansible_managed | comment }}
# worker MPM
# StartServers: initial number of server processes to start
# MaxClients: maximum number of simultaneous client connections
# MinSpareThreads: minimum number of worker threads which are kept spare
# MaxSpareThreads: maximum number of worker threads which are kept spare
# ThreadsPerChild: constant number of worker threads in each server process
# MaxRequestsPerChild: maximum number of requests a server process serves
<IfModule mpm_worker_module>
    StartServers           {{ apache2_mpm_startservers }}
    MinSpareThreads        {{ apache2_mpm_minsparethreads }}
    MaxSpareThreads        {{ apache2_mpm_maxsparethreads }}
    ThreadLimit            {{ apache2_mpm_threadlimit }}
    ThreadsPerChild        {{ apache2_mpm_threadsperchild }}
    ServerLimit            {{ apache2_mpm_serverlimit }}
    MaxRequestWorkers      {{ apache2_mpm_maxrequestworkers }}
    MaxConnectionsPerChild {{ apache2_mpm_maxconnectionsperchild }}
</IfModule>

# vim: syntax=apache ts=4 sw=4 sts=4 sr noet
